// import { permissions as userPermissions } from '../schema/user';
import { permissions as formPermissions } from '../schema/form';

export const permissions = [formPermissions];

export default {
  permissions
};
